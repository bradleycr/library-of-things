/**
 * Feature flags — single place to enable/disable optional features.
 * Set to false to hide ISBN-based checkout/return from nav, book page, and add-book success.
 */

export const ISBN_CHECKOUT_RETURN_ENABLED = true
