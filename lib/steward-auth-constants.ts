/** Shared constants for steward auth. Safe to import from Edge (middleware). */
export const STEWARD_SALT = "steward_salt_v1"
export const STEWARD_COOKIE_NAME = "steward_session"
/** Default password for local dev only. Override with STEWARD_PASSWORD. */
export const STEWARD_DEFAULT_PASSWORD = "password123"
