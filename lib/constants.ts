/**
 * App-wide limits used by both server and client.
 * Keep in sync when changing policy (e.g. max books per user).
 */

/** Maximum number of books a user may have checked out at once. */
export const MAX_BOOKS_CHECKED_OUT = 2
